{
  "name": "ERC721Metadata",
  "description": "The external contract that is responsible for generating metadata for the kitties, it has one function that will return the data as bytes.",

  "external-functions": [{
    "name": "getMetadata(uint256 _tokenId, string) public view returns (bytes32[4] buffer, uint256 count)",
    "description": "Given a token Id, returns a byte array that is supposed to be converted into string.",
    "params": [{
      "name": "_tokenId",
      "type": "uint256"
    }, {
      "type": "string"
    }],
    "returns": [{
        "name": "buffer",
        "type": "bytes32[4]"
      },
      {
        "name": "count",
        "type": "uint256"
      }
    ]
  }]
}